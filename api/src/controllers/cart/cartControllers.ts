import { Request, Response } from "express";

class Controller {
  /**
   * Update cart
   * URI => /api/v1/carts/update/:uuid
   * METHOD => PUT
   */
  public static async update(req: Request, res: Response) {}
}

export default Controller;
